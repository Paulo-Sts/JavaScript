// O slice remove uma seção do array e retorna o novo comprimento do array

let numeros = [1, 2, 3, 4, 5, 6]

numeros = numeros.slice(0, 3) // Inicia a remoção no índice 0 e remove até o índice 2

console.log(numeros)