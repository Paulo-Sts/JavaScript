// Esse método cria um iteravel com os valores de cada indice de um array

let carros = ['Gol', 'Fox', 'Polo', 'Uno', 'Golf', 'Virtus', 'Passat']

let valuesCarros = carros.values()

for (let i of valuesCarros) {
    console.log(i)
}

