//O undefined é definido quando uma variável não está inicializada com um valor
let valor
console.log(valor)

//O tipo null é usado para definir que uma variável não aponta para nenhum endereço de memória, ou seja, não referencia nada
valor = null
console.log(valor)